void setup() //create folder and sketch
{  // right click folder Example and git bash then type pwd
  //  git init (empty repo)
  // go on Git profile->repositories->new->same name as folder
  // click initialise this repo with a read me, add gitignore PROCESSING, license=MIT
  size(500,500); //git remote add origin (paste url after)
  // git pull origin master
} // git add .
 // git commit -m "text"
void draw() // git push OR git push --set-upstream origin master
{
  
  background(0);
  ellipse(width / 2, height / 2, 50, 50);
}
